<div class="step-content">
  <h2>Select Theater</h2>
  <% if @theaters.present? && @theaters.count > 0 %>
    <div class="theaters-grid">
      <% @theaters.each do |theater| %>
        <a href="/book/<%= @movie.id %>/date?theater_id=<%= theater.id %>" class="theater-card">
          <div class="theater-name"><%= theater.name %></div>
          <div class="theater-location"><%= theater.location %></div>
          <div class="theater-info"><%= theater.screens.count %> screens • <%= theater.total_capacity %> seats</div>
        </a>
      <% end %>
    </div>
  <% else %>
    <div class="empty-state">
      <h3>No theaters found in <%= session[:booking_city] %></h3>
      <p>Try selecting a different city.</p>
      <%= link_to "← Back to Cities", "/book/#{@movie.id}/city", class: "btn btn-secondary" %>
    </div>
  <% end %>
</div>
